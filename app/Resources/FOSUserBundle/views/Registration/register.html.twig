{% extends "@FOSUser/layout.html.twig" %}

{% block title %}Registraci&oacute;n{% endblock %}

{% block stylesheets %}
    {{ parent() }}

{#    <link rel="stylesheet" href="{{ asset('css/login.css') }}">#}
{% endblock %}

{% block fos_user_content %}
{% include "@FOSUser/Registration/register_content.html.twig" %}
{% endblock fos_user_content %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset("js/core.js") }}"></script>
    <script>
        $(function() {

            generate_captcha('lcaptcha', '{{ asset("images/recargar.png") }}');

            $(".fos_user_registration_register").submit(function (e) {
                // validacion captcha
                if (comprobarDatos() === true) {
                    $("form button[name='submit']").attr("value", "save");
                    return true;
                } else {
                    return false;
                }
            });

        });
    </script>

{% endblock %}