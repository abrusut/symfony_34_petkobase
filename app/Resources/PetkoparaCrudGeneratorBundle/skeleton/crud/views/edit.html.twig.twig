{{ "{% extends '"~  base_template  ~ "' %}" }}

{{ "{% block title %}" }}
{{ "{{ parent() }}" }} -  Edici&oacute;n de {{ entity_singularized }}
{{ "{% endblock %}" }}

{{ "{% block body %}" }}

<div class="row mt-5">
    {% include 'crud/views/others/flash_messages.html.twig.twig' %}
    <div class="col-lg-12">
        <h4>Edici&oacute;n de {{ entity_singularized }} <span class="fa fa-pencil" aria-hidden="true"></span> </h4>
    </div>
</div>

<div class="">
    {% include 'crud/views/others/flash_messages.html.twig.twig' %}

    {{ "{{ form_start(edit_form) }}" }}

    {{ "{{ form_widget(edit_form) }}" }}
    <p>
        <button type="submit" class="btn btn-primary">
            <span class="fa fa-check" aria-hidden="true"></span> Guardar
        </button>
    </p>
    {{ "{{ form_end(edit_form) }}" }}

    <hr/>

    {% set hide_edit, hide_delete, hide_new = true, false, false %}
{% include 'crud/views/others/record_actions.html.twig.twig' %}
</div>


{{ "{% endblock %}" }}

