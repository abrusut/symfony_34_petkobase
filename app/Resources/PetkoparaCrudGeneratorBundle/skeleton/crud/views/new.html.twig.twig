{{ "{% extends '"~  base_template  ~ "' %}" }}

{{ "{% block title %}" }}
{{ "{{ parent() }}" }} - Alta de {{ entity_singularized }}
{{ "{% endblock %}" }}

{{ "{% block body %}" }}

<div class="row mt-5">
    {% include 'crud/views/others/flash_messages.html.twig.twig' %}
    <div class="col-lg-12">
        <h4>Alta de {{ entity_singularized }} <span class="fa fa-file" aria-hidden="true"></span> </h4>
    </div>
</div>

<div class="">

    {{ "{{ form_start(form) }}" }}
    {{ "{{ form_widget(form) }}" }}
    <p>
        <button type="submit" name="submit" value="save" class="btn btn-primary">
            Guardar <span class="fa fa-check-circle" aria-hidden="true"></span>
        </button>
        <button type="submit" name="submit" value="saveAndAdd" class="btn btn-info">
            Guardar y Cargar Nuevo <span class="fa fa-plus" aria-hidden="true"></span>
        </button>
    </p>


    {{ "{{ form_end(form) }}" }}

    <hr/>
{% set hide_edit, hide_delete, hide_new = true, true, true %}
{% include 'crud/views/others/record_actions.html.twig.twig' %}
</div>


{{ "{% endblock %}" }}

