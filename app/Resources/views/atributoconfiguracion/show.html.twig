{% extends 'base.html.twig' %}

{% block title %}
{{ parent() }} - parametro de aplicaci&oacute;n
{% endblock %}

{% block body %}

<div class="row mt-5">
    <div class="col-md-12">
    {% for type, flashMessages in app.session.flashbag.all() %} 
    {% for flashMessage in flashMessages %}
    <div class="alert alert-{{ type }}">
        {{ flashMessage|raw }}
    </div>
     {% endfor %} 
     {% endfor %} 
</div>

    
    <div class="col-lg-12">
        <h4>Visualizaci&oacute;n de parametro de aplicaci&oacute;n <span class="fa fa-eye" aria-hidden="true"></span>  </h4>
    </div>
</div>

<div class="row">
    <div class="col-md-6">
        <p><strong>Id</strong></p>
        <p>
            {{ atributoConfiguracion.id }}
        </p>

    </div>
    <div class="col-md-6">
        <p><strong>Clave</strong></p>
        <p>
            {{ atributoConfiguracion.clave }}
        </p>

    </div>
    <div class="col-md-6">
        <p><strong>Valor</strong></p>
        <p>
            {{ atributoConfiguracion.valor }}
        </p>

    </div>
{#    <div class="col-md-6">#}
{#        <p><strong>Createdat</strong></p>#}
{#        <p>#}
{#            {% if atributoConfiguracion.createdAt %}{{ atributoConfiguracion.createdAt|date('Y-m-d H:i:s') }}{% endif %}#}
{#        </p>#}

{#    </div>#}
{#    <div class="col-md-6">#}
{#        <p><strong>Updatedat</strong></p>#}
{#        <p>#}
{#            {% if atributoConfiguracion.updatedAt %}{{ atributoConfiguracion.updatedAt|date('Y-m-d H:i:s') }}{% endif %}#}
{#        </p>#}

{#    </div>#}
{#    <div class="col-md-6">#}
{#        <p><strong>Createdby</strong></p>#}
{#        <p>#}
{#            {{ atributoConfiguracion.createdBy }}#}
{#        </p>#}

{#    </div>#}
{#    <div class="col-md-6">#}
{#        <p><strong>Updatedby</strong></p>#}
{#        <p>#}
{#            {{ atributoConfiguracion.updatedBy }}#}
{#        </p>#}

{#    </div>#}
</div>

<hr/>


<div class="form-group">
    <a class="btn btn-secondary" href="{{ path('atributoconfiguracion') }}">
        <span class="fa fa-list" aria-hidden="true"></span>
        Volver al listado
    </a>
    <a class="btn btn-primary" href="{{ path('atributoconfiguracion_edit', { 'id':  atributoConfiguracion.id }) }}">
        <span class="fa fa-pencil" aria-hidden="true"></span>
        Editar
    </a>
    <a class="btn btn-info" href="{{ path('atributoconfiguracion_new') }}">
        <span class="fa fa-plus" aria-hidden="true"></span>
        Nuevo
    </a>
    <form action="{{ path('atributoconfiguracion_delete', { 'id': atributoConfiguracion.id }) }}" method="post" style="display: inline-block">
        <input type="hidden" name="_method" value="DELETE" />
        {{ form_widget(delete_form) }}
        <button class="btn btn-danger" type="submit" onclick="return confirm('¿Está seguro?');">
            <span class="fa fa-times" aria-hidden="true"></span>
            Eliminar
        </button>
    </form>
</div>


{% endblock %}

