{% extends 'base.html.twig' %}

{% block title %}
{{ parent() }} -  Edici&oacute;n de parametro de aplicaci&oacute;n
{% endblock %}

{% block body %}

<div class="row mt-5">
    <div class="col-md-12">
    {% for type, flashMessages in app.session.flashbag.all() %} 
    {% for flashMessage in flashMessages %}
    <div class="alert alert-{{ type }}">
        {{ flashMessage|raw }}
    </div>
     {% endfor %} 
     {% endfor %} 
</div>

    
    <div class="col-lg-12">
        <h4>Edici&oacute;n de parametro de aplicaci&oacute;n <span class="fa fa-pencil" aria-hidden="true"></span> </h4>
    </div>
</div>

<div class="">
    <div class="col-md-12">
    {% for type, flashMessages in app.session.flashbag.all() %} 
    {% for flashMessage in flashMessages %}
    <div class="alert alert-{{ type }}">
        {{ flashMessage|raw }}
    </div>
     {% endfor %} 
     {% endfor %} 
</div>

    

    {{ form_start(edit_form) }}

    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn btn-primary">
            <span class="fa fa-check" aria-hidden="true"></span> Guardar
        </button>
    </p>
    {{ form_end(edit_form) }}

    <hr/>

    
<div class="form-group">
    <a class="btn btn-secondary" href="{{ path('atributoconfiguracion') }}">
        <span class="fa fa-list" aria-hidden="true"></span>
        Volver al listado
    </a>
    <a class="btn btn-info" href="{{ path('atributoconfiguracion_new') }}">
        <span class="fa fa-plus" aria-hidden="true"></span>
        Nuevo
    </a>
    <form action="{{ path('atributoconfiguracion_delete', { 'id': atributoConfiguracion.id }) }}" method="post" style="display: inline-block">
        <input type="hidden" name="_method" value="DELETE" />
        {{ form_widget(delete_form) }}
        <button class="btn btn-danger" type="submit" onclick="return confirm('¿Está seguro?');">
            <span class="fa fa-times" aria-hidden="true"></span>
            Eliminar
        </button>
    </form>
</div>

</div>


{% endblock %}

