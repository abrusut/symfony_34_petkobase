{% extends 'default/base_frontend.html.twig' %}

{% block body %}

    <div class="row mt-5">
        <div class="col-md-12">
            {% for type, flashMessages in app.session.flashbag.all() %}
                {% for flashMessage in flashMessages %}
                    <div class="alert alert-{{ type }}">
                        {{ flashMessage|raw }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        <div class="col-lg-12">
            <h4>Documentaci&oacute;n &Uacute;til Relacionada</h4>
            <p>En est&aacute; secci&oacute;n podr&aacute; encontrar informaci&oacute;n relacionada al programa.</p>
        </div>
    </div>

    <div class="row mt-5">
{#        <div class="col-lg-12">#}
{#            <p><a href={{ video_institucional }} target="_blank"> &rtrif; Precios Santafesinos - Video Institucional</a></p>#}
{#        </div>#}
{#        <div class="col-lg-12">#}
{#            <p><a href="{{ asset('uploads/manuales/GUIA_INSTRUCTIVA_2020_PRECIOS_SANTAFESINOS.pdf') }}" target="_blank"> &rtrif; Precios Santafesinos - Gu&iacute;a instructiva 2020</a></p>#}
{#        </div>#}
{#        <div class="col-lg-12">#}
{#            <p><a href="{{ asset('uploads/manuales/SENIALETICA_COMERCIOS_PRECIOS_SANTAFESINOS.pdf') }}" target="_blank"> &rtrif; Precios Santafesinos - Se&ntilde;aletica para comercios</a></p>#}
{#        </div>#}

    {% for contenido in contenidos %}
        <div class="col-lg-12">
            {% if contenido.content is defined  %}
                <p><a href="{{ asset('uploads/' ~ contenido.content) }}" target="_blank"> &rtrif; {{ contenido.acceso }}</a></p>
            {% else %} {# sino es un link absoluto #}
                <p><a href="{{ contenido.link }}" target="_blank"> &rtrif; {{ contenido.acceso }}</a></p>
            {% endif %}
        </div>
    {% endfor %}
    </div>

    <div class="row mt-5">
        <div class="col-lg-12">
            Para mayor informaci&oacute;n puede contactarse al <b>{{ contacto_telefono }}</b> o por correo electr&oacute;nico a <b>{{ contacto_mail }}</b>
        </div>
    </div>


{% endblock %}

{% block javascripts %}
    {{ parent() }}

{% endblock %}
